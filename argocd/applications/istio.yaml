# argocd/applications/istio.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexus-istio-service-mesh
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"  # Deploy Istio first
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ZakariaRek/gitops-repo_ArgoCD
    targetRevision: HEAD
    path: environments/dev/infrastructure/istio
  destination:
    server: https://kubernetes.default.svc
    namespace: istio-system
  syncPolicy:
    automated:
      prune: false  # ⚠️ CRITICAL: Never auto-prune Istio resources
      selfHeal: false  # ⚠️ CRITICAL: Disable self-heal for Istio
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true  # Required for Istio CRDs
      - ApplyOutOfSyncOnly=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 10  # Increased retry limit
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
  # Enhanced ignore differences for Istio
  ignoreDifferences:
    - group: install.istio.io
      kind: IstioOperator
      jsonPointers:
        - /spec/revision
        - /status
        - /spec/values/pilot/env
    - group: admissionregistration.k8s.io
      kind: MutatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/caBundle
        - /webhooks/1/clientConfig/caBundle
    - group: admissionregistration.k8s.io
      kind: ValidatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/caBundle
        - /webhooks/1/clientConfig/caBundle