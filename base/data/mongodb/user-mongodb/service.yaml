apiVersion: v1
kind: Service
metadata:
  name: user-mongodb-service
  labels:
    app: user-mongodb
    component: database
    service: user-service
spec:
  selector:
    app: user-mongodb
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongodb
  type: ClusterIP