apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: user-mongodb
  labels:
    app: user-mongodb
    component: database
    service: user-service

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml

commonLabels:
  app: user-mongodb
  component: database
  service: user-service

images:
  - name: mongo
    newTag: "7.0"