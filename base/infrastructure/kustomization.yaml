apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: dev-infrastructure
  namespace: dev-infrastructure

# Include all infrastructure components
resources:
- config-server
- eureka-server
# - api-gateway (when you create it)
# - ingress-nginx (when you create it)

# Common dev environment settings
commonLabels:
  environment: dev
  managed-by: argocd

namespace: dev-infrastructure