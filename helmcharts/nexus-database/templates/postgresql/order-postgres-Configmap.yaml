apiVersion: v1
kind: ConfigMap
metadata:
  name: order-postgres-config
  namespace: data
  labels:
    app: order-postgres
    component: data
    service: order-service
data:
  database-name: "orderdb"
