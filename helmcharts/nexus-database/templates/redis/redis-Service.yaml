apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: data
  labels:
    app: redis-service
    component: data
    tier: cache
spec:
  selector:
    app: redis-service
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP