apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kafka-zookeeper
#  - redis
  - user-mongodb

labels:
  - pairs:
      environment: dev
      managed-by: argocd

namespace: data